# Sprint 13 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Three operational improvements: (1) server-side pagination on the dealer list so the page doesn't load thousands of rows, (2) a dedicated overdue-payment action page so sales can act on overdue orders with one click, (3) an admin audit log viewer so ops can trace who changed what and when.

**Architecture:** All changes are in existing routers and templates. No new models. The dealer list already uses DataTables client-side — we replace that with proper server-side LIMIT/OFFSET + Bootstrap pagination, which also scopes the expensive outstanding_map join to just the current page. The audit log viewer is a new route in `routers/admin.py` and a new template. The overdue page is a new route in `routers/dealers.py` (registered before `/{dealer_id}` to avoid path collision).

**Tech Stack:** FastAPI, SQLAlchemy 2.0 async, Jinja2/Bootstrap 5, Python stdlib only

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `routers/dealers.py` | Modify | Paginate `list_dealers()`; add `GET /overdue` route |
| `templates/dealers/list.html` | Modify | Replace DataTables pagination with server-side Bootstrap pagination; add Overdue link |
| `templates/dealers/overdue.html` | Create | Overdue orders table with WhatsApp reminder action per row |
| `routers/admin.py` | Modify | Add `GET /admin/audit-log` route with filters + pagination |
| `templates/admin/audit_log.html` | Create | Paginated, filterable audit log table |
| `tests/test_sprint13_unit.py` | Create | Unit tests for all three features |

---

## Task 1: Server-side pagination on dealer list

**Files:**
- Modify: `routers/dealers.py` — `list_dealers()` function
- Modify: `templates/dealers/list.html` — remove DataTables paging, add Bootstrap pagination
- Create: `tests/test_sprint13_unit.py`

**Context:** `list_dealers()` currently does `query.order_by(Dealer.business_name)` with no LIMIT, loads every dealer into Python, then counts them with `len()`. The outstanding_map computes all dealer due_amounts in one GROUP BY query — efficient but redundant once paginated. With server-side pagination, we scope the outstanding_map to current page IDs only. The template uses DataTables with `pageLength:25, searching:false` — this adds JS overhead and double-pagination. We replace it with plain Bootstrap pagination links.

**Pagination parameters:** 50 dealers per page, `page` query param (default 1).

- [ ] **Step 1: Write failing tests**

Create `tests/test_sprint13_unit.py`:

```python
"""Sprint 13 unit tests — run without a database."""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))


def test_list_dealers_accepts_page_param():
    """list_dealers must accept a 'page' query parameter."""
    import inspect, importlib
    mod = importlib.import_module("routers.dealers")
    fn = getattr(mod, "list_dealers", None)
    assert fn is not None
    sig = inspect.signature(fn)
    assert "page" in sig.parameters, "list_dealers must have a 'page' parameter"


def test_pagination_offset_calculation():
    """Offset must be (page - 1) * per_page."""
    per_page = 50
    for page, expected_offset in [(1, 0), (2, 50), (3, 100), (10, 450)]:
        offset = (page - 1) * per_page
        assert offset == expected_offset, f"page={page}: expected {expected_offset}, got {offset}"


def test_total_pages_calculation():
    """total_pages must ceil-divide total_count by per_page."""
    import math
    per_page = 50
    cases = [(0, 1), (1, 1), (50, 1), (51, 2), (100, 2), (101, 3)]
    for total, expected in cases:
        pages = max(1, math.ceil(total / per_page))
        assert pages == expected, f"total={total}: expected {expected}, got {pages}"
```

- [ ] **Step 2: Run tests — expect first test to fail**

```
cd C:\Users\Pankaj.sehgal\Claude\Oxypc\oxypc-inventory
python -m pytest tests/test_sprint13_unit.py -v
```

Expected: `test_list_dealers_accepts_page_param` FAILS; other two PASS (pure logic)

- [ ] **Step 3: Add `import math` to routers/dealers.py**

Read `routers/dealers.py`. Find the stdlib imports block (`from datetime import datetime, date` etc). Add:
```python
import math
```

- [ ] **Step 4: Rewrite list_dealers() with server-side pagination**

Read `routers/dealers.py`. Find `list_dealers()`. Its signature currently is:

```python
async def list_dealers(
    request: Request,
    q: str = Query(default=""),
    status: str = Query(default=""),
    assigned: str = Query(default=""),
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
```

Replace the entire `list_dealers()` function body (keeping the decorator `@router.get("", response_class=HTMLResponse)`) with this implementation:

```python
PER_PAGE = 50


@router.get("", response_class=HTMLResponse)
async def list_dealers(
    request: Request,
    q: str = Query(default=""),
    status: str = Query(default=""),
    assigned: str = Query(default=""),
    page: int = Query(default=1, ge=1),
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    from sqlalchemy import or_
    base_query = select(Dealer)
    if q:
        like = f"%{q}%"
        base_query = base_query.where(or_(
            Dealer.business_name.ilike(like),
            Dealer.city.ilike(like),
            Dealer.phone.ilike(like),
            Dealer.contact_person.ilike(like),
        ))
    if status:
        base_query = base_query.where(Dealer.status == status)
    if assigned:
        base_query = base_query.where(Dealer.assigned_to == assigned)
    elif current_user.role in (UserRole.sales, UserRole.telecaller):
        base_query = base_query.where(Dealer.assigned_to == current_user.username)

    # Total count (all matching, not paginated)
    count_result = await db.execute(select(func.count()).select_from(base_query.subquery()))
    total_count = count_result.scalar() or 0
    total_pages = max(1, math.ceil(total_count / PER_PAGE))
    page = min(page, total_pages)

    # Active count (filtered, not paginated)
    active_q = base_query.where(Dealer.status == "active")
    active_result = await db.execute(select(func.count()).select_from(active_q.subquery()))
    active_count = active_result.scalar() or 0

    # Follow-ups due
    today = datetime.utcnow().date()
    fu_result = await db.execute(
        select(func.count(DealerCall.id)).where(
            func.date(DealerCall.next_followup_date) <= today,
            DealerCall.call_outcome != 'not_interested',
        )
    )
    followup_count = fu_result.scalar() or 0

    # Paginated dealer rows
    offset = (page - 1) * PER_PAGE
    dealers = (await db.execute(
        base_query.order_by(Dealer.business_name).offset(offset).limit(PER_PAGE)
    )).scalars().all()

    # Outstanding map — scoped to current page only for efficiency
    dealer_ids = [d.id for d in dealers]
    outstanding_map: dict = {}
    if dealer_ids:
        out_rows = await db.execute(
            select(DealerOrder.dealer_id, func.coalesce(func.sum(DealerOrder.due_amount), 0).label("out"))
            .where(DealerOrder.dealer_id.in_(dealer_ids), DealerOrder.status.in_(OUTSTANDING_STATUSES))
            .group_by(DealerOrder.dealer_id)
        )
        outstanding_map = {str(r.dealer_id): float(r.out) for r in out_rows}

    # Global outstanding total (all dealers, for summary card)
    out_total_result = await db.execute(
        select(func.coalesce(func.sum(DealerOrder.due_amount), 0))
        .where(DealerOrder.status.in_(OUTSTANDING_STATUSES))
    )
    outstanding = round(float(out_total_result.scalar() or 0))

    return templates.TemplateResponse("dealers/list.html", {
        "request": request,
        "current_user": current_user,
        "dealers": dealers,
        "q": q,
        "status": status,
        "assigned": assigned,
        "page": page,
        "total_pages": total_pages,
        "total_count": total_count,
        "active_count": active_count,
        "followup_count": followup_count,
        "outstanding": f"{outstanding:,}",
        "outstanding_map": outstanding_map,
    })
```

**Important:** Remove the old `from sqlalchemy import or_` that was inline inside the old function body (it's now at the top of `list_dealers`). Also remove the module-level `or_` if it was added there, to avoid duplication. Just keep the `from sqlalchemy import or_` inside `list_dealers` as shown above.

- [ ] **Step 5: Update templates/dealers/list.html**

Read `templates/dealers/list.html`.

**5a.** Replace the closing `{% block scripts %}` section. The current scripts block is:
```html
{% block scripts %}
<script>$(document).ready(function(){ $('#dealersTable').DataTable({order:[[1,'asc']],pageLength:25,searching:false}); });</script>
{% endblock %}
```

Replace with an empty scripts block (remove DataTables):
```html
{% block scripts %}{% endblock %}
```

**5b.** Insert a Bootstrap pagination control after the closing `</div>` of the dealers card (after `</div></div>` that closes card-body and card) and before `{% endblock %}`:

```html
{% if total_pages > 1 %}
<nav class="mt-3 d-flex justify-content-between align-items-center">
  <small class="text-muted">Showing {{ ((page-1)*50)+1 }}–{{ [page*50, total_count]|min }} of {{ total_count }} dealers</small>
  <ul class="pagination pagination-sm mb-0">
    {% if page > 1 %}
    <li class="page-item"><a class="page-link" href="?q={{ q }}&status={{ status }}&assigned={{ assigned }}&page={{ page - 1 }}">‹ Prev</a></li>
    {% endif %}
    {% for p in range(1, total_pages + 1) %}
    {% if p == page %}
    <li class="page-item active"><span class="page-link">{{ p }}</span></li>
    {% elif p == 1 or p == total_pages or (p >= page - 2 and p <= page + 2) %}
    <li class="page-item"><a class="page-link" href="?q={{ q }}&status={{ status }}&assigned={{ assigned }}&page={{ p }}">{{ p }}</a></li>
    {% elif p == page - 3 or p == page + 3 %}
    <li class="page-item disabled"><span class="page-link">…</span></li>
    {% endif %}
    {% endfor %}
    {% if page < total_pages %}
    <li class="page-item"><a class="page-link" href="?q={{ q }}&status={{ status }}&assigned={{ assigned }}&page={{ page + 1 }}">Next ›</a></li>
    {% endif %}
  </ul>
</nav>
{% endif %}
```

**5c.** Add an "Overdue" button to the action buttons row (next to "Follow-ups Due"). Find:
```html
    <a href="/dealers/followups-due" class="btn btn-sm btn-warning"><i class="bi bi-bell me-1"></i>Follow-ups Due <span class="badge bg-white text-warning">{{ followup_count }}</span></a>
```

Add after it:
```html
    <a href="/dealers/overdue" class="btn btn-sm btn-danger"><i class="bi bi-exclamation-triangle me-1"></i>Overdue Orders</a>
```

- [ ] **Step 6: Verify**

```
python -c "from routers.dealers import router; print('OK')"
```
Expected: `OK`

```
python -c "
from jinja2 import Environment, FileSystemLoader
env = Environment(loader=FileSystemLoader('templates'))
env.get_template('dealers/list.html')
print('Template OK')
"
```
Expected: `Template OK`

```
python -m pytest tests/test_sprint13_unit.py -v
```
Expected: all 3 tests PASS

- [ ] **Step 7: Commit**

```
git add routers/dealers.py templates/dealers/list.html tests/test_sprint13_unit.py
git commit -m "feat(sprint13): server-side pagination on dealer list — 50/page, scoped outstanding map"
```

---

## Task 2: Overdue payment alert page

**Files:**
- Modify: `routers/dealers.py` — add `GET /overdue` route (before `/{dealer_id}`)
- Create: `templates/dealers/overdue.html`
- Modify: `tests/test_sprint13_unit.py`

**Context:** The dashboard now shows `dealer_overdue_count` (Sprint 12) but clicking "View" just goes to `/dealers`. This task creates a dedicated `/dealers/overdue` page listing every overdue order with dealer name, amount due, days overdue, and a one-click WhatsApp reminder button. The route must be registered BEFORE `/{dealer_id}` in dealers.py — FastAPI matches routes in registration order, so static paths like `/overdue` must come before path parameters.

- [ ] **Step 1: Write failing test**

Add to `tests/test_sprint13_unit.py`:

```python
def test_overdue_route_exists():
    """dealers router must have a /overdue GET route."""
    import importlib
    mod = importlib.import_module("routers.dealers")
    paths = [r.path for r in mod.router.routes]
    assert "/overdue" in paths, f"No /overdue route. Routes: {paths}"
```

Run: `python -m pytest tests/test_sprint13_unit.py::test_overdue_route_exists -v`
Expected: FAIL

- [ ] **Step 2: Add overdue route to routers/dealers.py**

Read `routers/dealers.py`. Find the `followups_due` route (which correctly sits before `/{dealer_id}` routes). Add the overdue route immediately after `followups_due` and before any `/{dealer_id}` route:

```python
@router.get("/overdue", response_class=HTMLResponse)
async def overdue_orders(
    request: Request,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """List all dealer orders that are past their payment_due_date with amount still outstanding."""
    if current_user.role not in SALES_ROLES:
        return RedirectResponse(url="/?error=Access+denied", status_code=302)

    now = datetime.utcnow()
    rows = (await db.execute(
        select(DealerOrder, Dealer)
        .join(Dealer, DealerOrder.dealer_id == Dealer.id)
        .where(
            DealerOrder.due_amount > 0,
            DealerOrder.payment_due_date.isnot(None),
            DealerOrder.payment_due_date < now,
        )
        .order_by(DealerOrder.payment_due_date)
    )).all()

    overdue = []
    for order, dealer in rows:
        days_overdue = (now - order.payment_due_date).days
        overdue.append({
            "order": order,
            "dealer": dealer,
            "days_overdue": days_overdue,
        })

    return templates.TemplateResponse("dealers/overdue.html", {
        "request": request,
        "current_user": current_user,
        "overdue": overdue,
        "total_due": sum(float(item["order"].due_amount) for item in overdue),
    })
```

- [ ] **Step 3: Create templates/dealers/overdue.html**

Create `C:\Users\Pankaj.sehgal\Claude\Oxypc\oxypc-inventory\templates\dealers\overdue.html`:

```html
{% extends "base.html" %}
{% block title %}Overdue Orders — OxyPC{% endblock %}
{% block page_title %}Overdue Payment Orders{% endblock %}
{% block content %}

<div class="d-flex justify-content-between align-items-center mb-3">
  <div>
    <span class="text-muted small">{{ overdue|length }} overdue order(s) — Total outstanding: <strong class="text-danger">₹{{ "{:,.0f}".format(total_due) }}</strong></span>
  </div>
  <a href="/dealers" class="btn btn-sm btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Back to Dealers</a>
</div>

{% if not overdue %}
<div class="alert alert-success border-0 shadow-sm">
  <i class="bi bi-check-circle me-2"></i>No overdue orders — all payments are current.
</div>
{% else %}
<div class="card border-0 shadow-sm">
  <div class="card-body p-0">
    <div class="table-responsive">
      <table class="table table-hover mb-0 small">
        <thead class="table-light">
          <tr>
            <th>Dealer</th>
            <th>Order#</th>
            <th>Order Date</th>
            <th>Due Date</th>
            <th>Days Overdue</th>
            <th>Amount Due</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          {% for item in overdue %}
          {% set order = item.order %}
          {% set dealer = item.dealer %}
          {% set days = item.days_overdue %}
          <tr>
            <td>
              <a href="/dealers/{{ dealer.id }}" class="fw-semibold text-decoration-none">{{ dealer.business_name }}</a>
              <div class="text-muted" style="font-size:.75rem">{{ dealer.phone or '' }}</div>
            </td>
            <td><code>{{ order.order_number }}</code></td>
            <td>{{ order.order_date.strftime('%d-%m-%Y') }}</td>
            <td class="text-danger">{{ order.payment_due_date.strftime('%d-%m-%Y') }}</td>
            <td>
              <span class="badge {% if days > 30 %}bg-danger{% elif days > 7 %}bg-warning text-dark{% else %}bg-secondary{% endif %}">
                {{ days }}d overdue
              </span>
            </td>
            <td class="fw-semibold text-danger">₹{{ "{:,.0f}".format(order.due_amount) }}</td>
            <td>
              <div class="d-flex gap-1">
                <a href="/dealers/{{ dealer.id }}" class="btn btn-xs btn-outline-primary" title="View dealer"><i class="bi bi-eye"></i></a>
                <a href="/whatsapp/compose?dealer_id={{ dealer.id }}&type=payment_reminder&order_id={{ order.id }}"
                   class="btn btn-xs btn-outline-warning" title="Send WhatsApp reminder"><i class="bi bi-whatsapp"></i></a>
              </div>
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>
</div>
{% endif %}

{% endblock %}
```

- [ ] **Step 4: Verify**

```
python -c "from routers.dealers import router; print('OK')"
```

```
python -c "
from jinja2 import Environment, FileSystemLoader
env = Environment(loader=FileSystemLoader('templates'))
env.get_template('dealers/overdue.html')
print('Template OK')
"
```

```
python -m pytest tests/test_sprint13_unit.py -v
```
Expected: 4 tests PASS

- [ ] **Step 5: Commit**

```
git add routers/dealers.py templates/dealers/overdue.html tests/test_sprint13_unit.py
git commit -m "feat(sprint13): overdue payment alert page at /dealers/overdue with WhatsApp reminder"
```

---

## Task 3: Admin audit log viewer

**Files:**
- Modify: `routers/admin.py` — add `GET /admin/audit-log`
- Create: `templates/admin/audit_log.html`
- Modify: `tests/test_sprint13_unit.py`

**Context:** The `AuditLog` model (in `models/engines.py`) already has data written by `services/audit_engine.py` for every write operation across the system. The table has: `timestamp`, `username`, `action`, `table_name`, `record_id`, `old_value`, `new_value`, `ip_address`, `notes`. No router currently reads this table — there is no admin UI to browse it. This task adds a paginated, filterable viewer accessible only to admins.

- [ ] **Step 1: Write failing test**

Add to `tests/test_sprint13_unit.py`:

```python
def test_admin_audit_log_route_exists():
    """admin router must have an /audit-log GET route."""
    import importlib
    mod = importlib.import_module("routers.admin")
    paths = [r.path for r in mod.router.routes]
    assert any("audit" in p for p in paths), \
        f"No audit-log route in admin router. Routes: {paths}"
```

Run: `python -m pytest tests/test_sprint13_unit.py::test_admin_audit_log_route_exists -v`
Expected: FAIL

- [ ] **Step 2: Add AuditLog import to routers/admin.py**

Read `routers/admin.py`. Add to the model imports:
```python
from models.engines import AuditLog
```

Also ensure `import math` is present (add if not). Also add `Query` to the FastAPI imports if not already there:
```python
from fastapi import APIRouter, Depends, Form, Request, HTTPException, Query
```

- [ ] **Step 3: Add audit log route to routers/admin.py**

Add this route to `routers/admin.py` (append after the last existing route):

```python
AUDIT_PER_PAGE = 50


@router.get("/audit-log", response_class=HTMLResponse)
async def audit_log_view(
    request: Request,
    username: str = Query(default=""),
    action: str = Query(default=""),
    table_name: str = Query(default=""),
    page: int = Query(default=1, ge=1),
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    if current_user.role != UserRole.admin:
        return RedirectResponse(url="/?error=Admin+only", status_code=302)

    q = select(AuditLog)
    if username:
        q = q.where(AuditLog.username.ilike(f"%{username}%"))
    if action:
        q = q.where(AuditLog.action.ilike(f"%{action}%"))
    if table_name:
        q = q.where(AuditLog.table_name == table_name)

    # Total count for pagination
    count_result = await db.execute(select(func.count()).select_from(q.subquery()))
    total_count = count_result.scalar() or 0
    import math as _math
    total_pages = max(1, _math.ceil(total_count / AUDIT_PER_PAGE))
    page = min(page, total_pages)

    logs = (await db.execute(
        q.order_by(AuditLog.timestamp.desc())
         .offset((page - 1) * AUDIT_PER_PAGE)
         .limit(AUDIT_PER_PAGE)
    )).scalars().all()

    # Distinct table names for the filter dropdown
    table_names_result = await db.execute(
        select(AuditLog.table_name).distinct().order_by(AuditLog.table_name)
    )
    table_names = [r[0] for r in table_names_result.all() if r[0]]

    return templates.TemplateResponse("admin/audit_log.html", {
        "request": request,
        "current_user": current_user,
        "logs": logs,
        "page": page,
        "total_pages": total_pages,
        "total_count": total_count,
        "username": username,
        "action": action,
        "table_name": table_name,
        "table_names": table_names,
        "per_page": AUDIT_PER_PAGE,
    })
```

- [ ] **Step 4: Create templates/admin/audit_log.html**

Create `C:\Users\Pankaj.sehgal\Claude\Oxypc\oxypc-inventory\templates\admin\audit_log.html`:

```html
{% extends "base.html" %}
{% block title %}Audit Log — OxyPC Admin{% endblock %}
{% block page_title %}Audit Log{% endblock %}
{% block content %}

<!-- Filter form -->
<div class="card border-0 shadow-sm mb-3">
  <div class="card-body py-2">
    <form method="get" class="d-flex gap-2 flex-wrap align-items-end">
      <div>
        <label class="form-label small fw-semibold mb-1">Username</label>
        <input type="text" name="username" value="{{ username }}" class="form-control form-control-sm" style="width:150px" placeholder="Search user…">
      </div>
      <div>
        <label class="form-label small fw-semibold mb-1">Action</label>
        <input type="text" name="action" value="{{ action }}" class="form-control form-control-sm" style="width:200px" placeholder="e.g. ORDER_CREATED">
      </div>
      <div>
        <label class="form-label small fw-semibold mb-1">Table</label>
        <select name="table_name" class="form-select form-select-sm" style="width:180px">
          <option value="">All Tables</option>
          {% for t in table_names %}
          <option value="{{ t }}" {% if t == table_name %}selected{% endif %}>{{ t }}</option>
          {% endfor %}
        </select>
      </div>
      <div class="d-flex gap-2">
        <button class="btn btn-sm btn-primary">Filter</button>
        <a href="/admin/audit-log" class="btn btn-sm btn-outline-secondary">Clear</a>
      </div>
    </form>
  </div>
</div>

<div class="d-flex justify-content-between align-items-center mb-2">
  <small class="text-muted">{{ total_count }} log entries</small>
</div>

<div class="card border-0 shadow-sm">
  <div class="card-body p-0">
    <div class="table-responsive">
      <table class="table table-hover table-sm mb-0 small">
        <thead class="table-dark">
          <tr>
            <th>Timestamp</th>
            <th>User</th>
            <th>Action</th>
            <th>Table</th>
            <th>Record ID</th>
            <th>Notes / Summary</th>
            <th>IP</th>
          </tr>
        </thead>
        <tbody>
          {% for log in logs %}
          <tr>
            <td class="text-muted" style="white-space:nowrap">{{ log.timestamp.strftime('%d-%m-%Y %H:%M:%S') }}</td>
            <td class="fw-semibold">{{ log.username or '—' }}</td>
            <td>
              {% set action_color = {
                'ORDER_CREATED': 'success', 'PAYMENT_RECORDED': 'primary',
                'CREDIT_NOTE_ISSUED': 'warning', 'SUPPLIER_PAYMENT_RECORDED': 'info',
                'RECEIPT_RECORDED': 'info', 'REPAIR_COMPLETE': 'secondary',
                'AUTO_SCRAP': 'danger', 'MANUAL_SCRAP': 'danger',
                'STAGE_MOVED': 'secondary'
              } %}
              <span class="badge bg-{{ action_color.get(log.action, 'secondary') }}">{{ log.action }}</span>
            </td>
            <td><code class="small">{{ log.table_name or '—' }}</code></td>
            <td><code class="small text-muted">{{ (log.record_id or '—')[:12] }}</code></td>
            <td class="text-muted" style="max-width:300px">
              {% if log.notes %}{{ log.notes[:120] }}{% elif log.new_value %}{{ log.new_value[:100] }}{% else %}—{% endif %}
            </td>
            <td class="text-muted small">{{ log.ip_address or '—' }}</td>
          </tr>
          {% else %}
          <tr><td colspan="7" class="text-center text-muted py-4">No audit log entries found.</td></tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>
</div>

<!-- Pagination -->
{% if total_pages > 1 %}
<nav class="mt-3 d-flex justify-content-between align-items-center">
  <small class="text-muted">Page {{ page }} of {{ total_pages }}</small>
  <ul class="pagination pagination-sm mb-0">
    {% if page > 1 %}
    <li class="page-item"><a class="page-link" href="?username={{ username }}&action={{ action }}&table_name={{ table_name }}&page={{ page - 1 }}">‹ Prev</a></li>
    {% endif %}
    {% for p in range(1, total_pages + 1) %}
    {% if p == page %}
    <li class="page-item active"><span class="page-link">{{ p }}</span></li>
    {% elif p == 1 or p == total_pages or (p >= page - 2 and p <= page + 2) %}
    <li class="page-item"><a class="page-link" href="?username={{ username }}&action={{ action }}&table_name={{ table_name }}&page={{ p }}">{{ p }}</a></li>
    {% elif p == page - 3 or p == page + 3 %}
    <li class="page-item disabled"><span class="page-link">…</span></li>
    {% endif %}
    {% endfor %}
    {% if page < total_pages %}
    <li class="page-item"><a class="page-link" href="?username={{ username }}&action={{ action }}&table_name={{ table_name }}&page={{ page + 1 }}">Next ›</a></li>
    {% endif %}
  </ul>
</nav>
{% endif %}

{% endblock %}
```

- [ ] **Step 5: Verify**

```
python -c "from routers.admin import router; print('OK')"
```

```
python -c "
from jinja2 import Environment, FileSystemLoader
env = Environment(loader=FileSystemLoader('templates'))
env.get_template('admin/audit_log.html')
print('Template OK')
"
```

```
python -m pytest tests/test_sprint13_unit.py -v
```
Expected: all 5 tests PASS

- [ ] **Step 6: Commit**

```
git add routers/admin.py templates/admin/audit_log.html tests/test_sprint13_unit.py
git commit -m "feat(sprint13): admin audit log viewer at /admin/audit-log — filterable, paginated"
```

---

## Self-Review

### Spec Coverage

| Feature | Task | Covered |
|---|---|---|
| Dealer list pagination (server-side) | Task 1 | ✅ LIMIT/OFFSET + Bootstrap pagination controls |
| Outstanding map scoped to current page | Task 1 | ✅ `.where(DealerOrder.dealer_id.in_(dealer_ids))` |
| Overdue orders page | Task 2 | ✅ `/dealers/overdue` with days overdue + WhatsApp action |
| Overdue link from dealer list | Task 1 | ✅ Overdue button added to list.html action bar |
| Audit log viewer | Task 3 | ✅ `/admin/audit-log` with filters + pagination |

### Placeholder scan

None — all steps contain exact code, exact file paths, exact expected output.

### Type consistency

- `page: int = Query(default=1, ge=1)` — clamp with `min(page, total_pages)` prevents OOB pages
- `PER_PAGE = 50` — module-level constant in dealers.py; `AUDIT_PER_PAGE = 50` in admin.py (separate constants for independent tunability)
- `(now - order.payment_due_date).days` — both are `datetime` objects; subtraction produces `timedelta`; `.days` gives the integer count
- `DealerOrder.dealer_id.in_(dealer_ids)` — `dealer_ids` is a list of UUID objects from `d.id`; SQLAlchemy handles UUID binding correctly
- `[r[0] for r in table_names_result.all() if r[0]]` — `.all()` returns `Row` objects; `r[0]` is the first (only) column value; filter removes `None` rows
