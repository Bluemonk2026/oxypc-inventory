# Sprint 14 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Three delivery improvements: (1) add two missing nav links to the sidebar ("Overdue Orders" and "Audit Log") so users can reach them directly, (2) add a `GET /whatsapp/compose` route and template that pre-fills the WhatsApp send form from an order's data so the one-click reminder flow lands on a review screen rather than firing blindly, (3) add a `GET /dealers/ageing` route and template that buckets outstanding dealer payments into time bands (current / 1-30 / 31-60 / 61-90 / 90+) with CSV export.

**Architecture:** All changes are in existing files. No new models, no migrations. The nav links are pure template edits. The compose page is a new `GET` route inserted before existing WhatsApp routes (imports and helpers are already present). The ageing report is a new `GET` route inserted between `/dealers/overdue` and `/dealers/new` (the existing safe position for non-ID sub-paths). Bucketing is done in Python — no SQL CASE expressions — to keep the query simple and the logic easy to test without a DB.

**Tech Stack:** FastAPI, SQLAlchemy 2.0 async (asyncpg), PostgreSQL 15, Jinja2, Bootstrap 5, Python stdlib (`csv`, `io`, `datetime`).

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `templates/base.html` | Modify | Add "Overdue Orders" link after Dealers (line ~149); add "Audit Log" link in Admin section before `{% endif %}` (line ~288) |
| `routers/whatsapp.py` | Modify | Add `GET /whatsapp/compose` route |
| `templates/whatsapp/compose.html` | Create | Pre-filled compose form; POSTs to `/whatsapp/send` |
| `routers/dealers.py` | Modify | Add `GET /dealers/ageing` route between `/overdue` and `/new` |
| `templates/dealers/ageing.html` | Create | Ageing analysis table with CSV export button and totals footer |
| `tests/test_sprint14_unit.py` | Create | 5 unit tests (no DB required) |

---

## Task 1: Sidebar nav links

**Files:**
- Modify: `templates/base.html`
- Add to: `tests/test_sprint14_unit.py`

**Context:** The sidebar Dealers section (lines 131-159) wraps all dealers/telecalling/whatsapp links inside `{% if role in ['admin','sales','sales_manager','telecaller'] %}`. The existing Dealers link is at lines 147-149. Telecalling immediately follows at lines 150-152. We add an "Overdue Orders" `<li>` between them — it inherits the same role guard from the enclosing block. The Admin section (lines 262-289) ends with the stage-control "Audit Log" link at lines 286-288 then `{% endif %}`. We insert an admin-only Audit Log link for `/admin/audit-log` right after line 288, before the `{% endif %}`.

- [ ] **Step 1.1: Write failing test**

Create `tests/test_sprint14_unit.py`:

```python
"""Sprint 14 unit tests — run without a database."""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))


# ─── Task 1: base.html nav links ──────────────────────────────────────────────

def test_base_html_overdue_nav_link():
    """base.html must contain an 'Overdue Orders' link to /dealers/overdue."""
    base = os.path.join(os.path.dirname(__file__), "..", "templates", "base.html")
    with open(base, encoding="utf-8") as f:
        content = f.read()
    assert 'href="/dealers/overdue"' in content, \
        "base.html missing href=/dealers/overdue nav link"
    assert "Overdue Orders" in content, \
        "base.html missing 'Overdue Orders' link text"


def test_base_html_audit_log_nav_link():
    """base.html must contain an 'Audit Log' link to /admin/audit-log."""
    base = os.path.join(os.path.dirname(__file__), "..", "templates", "base.html")
    with open(base, encoding="utf-8") as f:
        content = f.read()
    assert 'href="/admin/audit-log"' in content, \
        "base.html missing href=/admin/audit-log nav link"


# ─── Task 2: WhatsApp compose route ───────────────────────────────────────────

def test_whatsapp_compose_route_exists():
    """whatsapp router must have a GET /whatsapp/compose route."""
    import importlib
    mod = importlib.import_module("routers.whatsapp")
    paths = [r.path for r in mod.router.routes]
    assert "/whatsapp/compose" in paths, \
        f"No /whatsapp/compose route found. Routes: {paths}"


# ─── Task 3: Dealer ageing route ──────────────────────────────────────────────

def test_dealers_ageing_route_exists():
    """dealers router must have a GET /dealers/ageing route."""
    import importlib
    mod = importlib.import_module("routers.dealers")
    paths = [r.path for r in mod.router.routes]
    assert "/dealers/ageing" in paths, \
        f"No /dealers/ageing route found. Routes: {paths}"


def test_ageing_bucket_logic():
    """Bucket math: current / 1-30 / 31-60 / 61-90 / 90+."""
    from datetime import datetime, timedelta

    now = datetime(2026, 4, 26, 12, 0, 0)

    class FakeOrder:
        def __init__(self, due_date, amount):
            self.payment_due_date = due_date
            self.due_amount = amount

    cases = [
        # (payment_due_date, amount, expected_bucket)
        (now + timedelta(days=5),    500.0, "current"),    # future → current
        (now,                        100.0, "current"),    # same moment → current
        (now - timedelta(days=1),    200.0, "d30"),        # 1 day overdue → 1-30
        (now - timedelta(days=30),   300.0, "d30"),        # exactly 30 → 1-30
        (now - timedelta(days=31),   400.0, "d60"),        # 31 → 31-60
        (now - timedelta(days=60),   500.0, "d60"),        # exactly 60 → 31-60
        (now - timedelta(days=61),   600.0, "d90"),        # 61 → 61-90
        (now - timedelta(days=90),   700.0, "d90"),        # exactly 90 → 61-90
        (now - timedelta(days=91),   800.0, "d90plus"),    # 91+ → 90+
        (now - timedelta(days=365), 1000.0, "d90plus"),    # very old → 90+
        (None,                       150.0, "current"),    # no due date → current
    ]

    for due_date, amount, expected in cases:
        order = FakeOrder(due_date, amount)
        amt = float(order.due_amount)
        if order.payment_due_date is None or order.payment_due_date >= now:
            bucket = "current"
        else:
            days = (now - order.payment_due_date).days
            if days <= 30:
                bucket = "d30"
            elif days <= 60:
                bucket = "d60"
            elif days <= 90:
                bucket = "d90"
            else:
                bucket = "d90plus"
        assert bucket == expected, \
            f"due={due_date}, amount={amount}: expected {expected}, got {bucket}"
        assert amt == amount  # amount preserved
```

**Run (must FAIL before changes):**
```
cd C:\Users\Pankaj.sehgal\Claude\Oxypc\oxypc-inventory
python -m pytest tests/test_sprint14_unit.py -v 2>&1 | head -40
```

**Expected output (all 5 fail):**
```
FAILED tests/test_sprint14_unit.py::test_base_html_overdue_nav_link
FAILED tests/test_sprint14_unit.py::test_base_html_audit_log_nav_link
FAILED tests/test_sprint14_unit.py::test_whatsapp_compose_route_exists
FAILED tests/test_sprint14_unit.py::test_dealers_ageing_route_exists
FAILED tests/test_sprint14_unit.py::test_ageing_bucket_logic  ← this one actually PASSES already (pure logic)
```

Note: `test_ageing_bucket_logic` contains only pure Python math — it will pass before any file changes. All four structural tests should fail.

- [ ] **Step 1.2: Add "Overdue Orders" link to sidebar**

In `templates/base.html`, locate the existing Dealers link block (around line 147):

```html
      <li><a href="/dealers" class="nav-link {% if request.url.path.startswith('/dealers') %}active{% endif %}">
        <i class="bi bi-building me-2"></i> Dealers
      </a></li>
      <li><a href="/telecalling" class="nav-link {% if request.url.path.startswith('/telecalling') %}active{% endif %}">
```

Insert the following `<li>` **between** the Dealers link and the Telecalling link:

```html
      <li><a href="/dealers/overdue" class="nav-link {% if '/dealers/overdue' in request.url.path %}active{% endif %}">
        <i class="bi bi-exclamation-circle me-2 text-danger"></i> Overdue Orders
      </a></li>
```

After the edit that section reads:

```html
      <li><a href="/dealers" class="nav-link {% if request.url.path.startswith('/dealers') %}active{% endif %}">
        <i class="bi bi-building me-2"></i> Dealers
      </a></li>
      <li><a href="/dealers/overdue" class="nav-link {% if '/dealers/overdue' in request.url.path %}active{% endif %}">
        <i class="bi bi-exclamation-circle me-2 text-danger"></i> Overdue Orders
      </a></li>
      <li><a href="/telecalling" class="nav-link {% if request.url.path.startswith('/telecalling') %}active{% endif %}">
        <i class="bi bi-telephone me-2"></i> Telecalling
      </a></li>
```

- [ ] **Step 1.3: Add "Audit Log" link in Admin section**

In `templates/base.html`, locate the end of the Admin section (around lines 286-289):

```html
      <li><a href="/stage-control/audit" class="nav-link {% if '/stage-control/audit' in request.url.path %}active{% endif %}">
        <i class="bi bi-journal-text me-2"></i> Audit Log
      </a></li>
      {% endif %}
```

Insert the following `<li>` **after** the stage-control Audit Log `</li>` and **before** `{% endif %}`:

```html
      <li><a href="/admin/audit-log" class="nav-link {% if '/admin/audit-log' in request.url.path %}active{% endif %}">
        <i class="bi bi-shield-check me-2"></i> Audit Log
      </a></li>
```

After the edit that section reads:

```html
      <li><a href="/stage-control/audit" class="nav-link {% if '/stage-control/audit' in request.url.path %}active{% endif %}">
        <i class="bi bi-journal-text me-2"></i> Audit Log
      </a></li>
      <li><a href="/admin/audit-log" class="nav-link {% if '/admin/audit-log' in request.url.path %}active{% endif %}">
        <i class="bi bi-shield-check me-2"></i> Audit Log
      </a></li>
      {% endif %}
```

- [ ] **Step 1.4: Run tests (Task 1 tests must now PASS)**

```
cd C:\Users\Pankaj.sehgal\Claude\Oxypc\oxypc-inventory
python -m pytest tests/test_sprint14_unit.py::test_base_html_overdue_nav_link tests/test_sprint14_unit.py::test_base_html_audit_log_nav_link -v
```

**Expected output:**
```
PASSED tests/test_sprint14_unit.py::test_base_html_overdue_nav_link
PASSED tests/test_sprint14_unit.py::test_base_html_audit_log_nav_link
2 passed in 0.xxs
```

---

## Task 2: WhatsApp payment reminder compose page

**Files:**
- Modify: `routers/whatsapp.py`
- Create: `templates/whatsapp/compose.html`

**Context:** The existing `POST /whatsapp/send-reminder/{order_id}` sends a WhatsApp reminder directly with no user confirmation. The compose page provides a review screen — phone and message are pre-filled and editable before the user clicks Send. The POST target is the already-existing `POST /whatsapp/send` route. All required imports (`Query`, `select`, `Dealer`, `DealerOrder`, `require_roles`) are already present in `routers/whatsapp.py`.

- [ ] **Step 2.1: Add `GET /whatsapp/compose` route**

In `routers/whatsapp.py`, locate the main page route decorator `@router.get("", response_class=HTMLResponse)` (around line 78). Insert the new compose route **before** this existing route. The final order of GET routes should be: `/whatsapp/compose`, then `""` (main page).

Add this block immediately before `# ── Main page ──────────────────────────────────────────────────────────────`:

```python
# ── Compose / pre-fill page ────────────────────────────────────────────────
@router.get("/compose", response_class=HTMLResponse)
async def compose(
    request: Request,
    order_id: str = Query(default=""),
    type: str = Query(default=""),
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """Pre-fill the WhatsApp send form from a dealer order."""
    if not order_id:
        return RedirectResponse(url="/whatsapp?error=No+order+specified", status_code=302)

    order_result = await db.execute(
        select(DealerOrder).where(DealerOrder.id == order_id)
    )
    order = order_result.scalar_one_or_none()
    if order is None:
        return RedirectResponse(url="/whatsapp?error=Order+not+found", status_code=302)

    dealer_result = await db.execute(
        select(Dealer).where(Dealer.id == order.dealer_id)
    )
    dealer = dealer_result.scalar_one_or_none()
    if dealer is None:
        return RedirectResponse(url="/whatsapp?error=Dealer+not+found", status_code=302)

    prefill_phone = dealer.whatsapp_number or dealer.phone or ""
    greeting_name = dealer.first_name or dealer.business_name
    prefill_message = (
        f"Dear {greeting_name},\n\n"
        f"This is a payment reminder for invoice "
        f"{order.invoice_number or order.order_number}.\n"
        f"Outstanding amount: ₹{int(order.due_amount):,}\n\n"
        f"Please arrange payment at your earliest convenience."
        f"\n\nRegards,\nOxyPC Team"
    )

    return templates.TemplateResponse("whatsapp/compose.html", {
        "request": request,
        "current_user": current_user,
        "dealer": dealer,
        "order": order,
        "prefill_phone": prefill_phone,
        "prefill_message": prefill_message,
    })
```

- [ ] **Step 2.2: Create `templates/whatsapp/compose.html`**

Create the file `templates/whatsapp/compose.html` with the following content:

```html
{% extends "base.html" %}
{% block title %}Compose Payment Reminder{% endblock %}

{% block content %}
<div class="container-fluid py-4">
  <div class="row justify-content-center">
    <div class="col-lg-7 col-md-9">

      <!-- Header -->
      <div class="d-flex align-items-center mb-4">
        <a href="/dealers/overdue" class="btn btn-sm btn-outline-secondary me-3">
          <i class="bi bi-arrow-left"></i> Back to Overdue Orders
        </a>
        <div>
          <h4 class="mb-0 fw-semibold">
            <i class="bi bi-whatsapp text-success me-2"></i>Compose Payment Reminder
          </h4>
          <small class="text-muted">Review and edit before sending</small>
        </div>
      </div>

      <!-- Order Info (read-only) -->
      <div class="card mb-4 border-0 shadow-sm">
        <div class="card-header bg-light fw-semibold py-2">
          <i class="bi bi-receipt me-1"></i> Order Details
        </div>
        <div class="card-body">
          <div class="row g-2">
            <div class="col-sm-6">
              <span class="text-muted small">Dealer</span>
              <div class="fw-semibold">{{ dealer.business_name }}</div>
            </div>
            <div class="col-sm-6">
              <span class="text-muted small">Contact</span>
              <div>{{ dealer.first_name or "—" }}</div>
            </div>
            <div class="col-sm-6">
              <span class="text-muted small">Order / Invoice</span>
              <div class="fw-semibold">{{ order.invoice_number or order.order_number or "—" }}</div>
            </div>
            <div class="col-sm-6">
              <span class="text-muted small">Amount Due</span>
              <div class="fw-bold text-danger">&#8377;{{ "{:,.0f}".format(order.due_amount) }}</div>
            </div>
          </div>
        </div>
      </div>

      <!-- Warning: no phone on record -->
      {% if not prefill_phone %}
      <div class="alert alert-warning d-flex align-items-start mb-4" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-2 mt-1 flex-shrink-0"></i>
        <div>
          <strong>No phone number on record</strong> for this dealer.
          Enter a WhatsApp number below before sending.
        </div>
      </div>
      {% endif %}

      <!-- Send Form -->
      <div class="card border-0 shadow-sm">
        <div class="card-header bg-light fw-semibold py-2">
          <i class="bi bi-send me-1"></i> Send via WhatsApp
        </div>
        <div class="card-body">
          <form method="POST" action="/whatsapp/send">
            <!-- Hidden fields -->
            <input type="hidden" name="dealer_id" value="{{ dealer.id }}">
            <input type="hidden" name="message_type" value="reminder">

            <!-- Recipient phone -->
            <div class="mb-3">
              <label for="recipient_phone" class="form-label fw-semibold">
                Recipient WhatsApp Number <span class="text-danger">*</span>
              </label>
              <div class="input-group">
                <span class="input-group-text"><i class="bi bi-telephone"></i></span>
                <input
                  type="tel"
                  class="form-control {% if not prefill_phone %}border-warning{% endif %}"
                  id="recipient_phone"
                  name="recipient_phone"
                  value="{{ prefill_phone }}"
                  placeholder="e.g. 919876543210 (with country code)"
                  required
                >
              </div>
              <div class="form-text">Include country code without +. E.g. 91XXXXXXXXXX for India.</div>
            </div>

            <!-- Message text -->
            <div class="mb-4">
              <label for="message_text" class="form-label fw-semibold">Message</label>
              <textarea
                class="form-control font-monospace"
                id="message_text"
                name="message_text"
                rows="9"
                required
              >{{ prefill_message }}</textarea>
              <div class="form-text">You may edit the message before sending.</div>
            </div>

            <!-- Actions -->
            <div class="d-flex gap-2">
              <button type="submit" class="btn btn-success">
                <i class="bi bi-whatsapp me-1"></i> Send Message
              </button>
              <a href="/dealers/overdue" class="btn btn-outline-secondary">Cancel</a>
            </div>
          </form>
        </div>
      </div>

    </div>
  </div>
</div>
{% endblock %}
```

- [ ] **Step 2.3: Run Task 2 test**

```
cd C:\Users\Pankaj.sehgal\Claude\Oxypc\oxypc-inventory
python -m pytest tests/test_sprint14_unit.py::test_whatsapp_compose_route_exists -v
```

**Expected output:**
```
PASSED tests/test_sprint14_unit.py::test_whatsapp_compose_route_exists
1 passed in 0.xxs
```

---

## Task 3: Dealer ageing report

**Files:**
- Modify: `routers/dealers.py`
- Create: `templates/dealers/ageing.html`

**Context:** The `/dealers/ageing` route must be registered **between** `/dealers/overdue` (around line 153) and `/dealers/new` (around line 192) in `routers/dealers.py`. If it were placed after `/{dealer_id}`, FastAPI would try to match `ageing` as a `dealer_id` path param and 404. The imports `csv`, `io`, `StreamingResponse`, `OUTSTANDING_STATUSES`, `SALES_ROLES`, and `datetime` are all already present in the file.

- [ ] **Step 3.1: Add `GET /dealers/ageing` route**

In `routers/dealers.py`, locate the blank line separating `/overdue` from `/new` (after the closing `return templates.TemplateResponse("dealers/overdue.html", {...})` block and before `@router.get("/new", ...)`). Insert the full ageing route there:

```python
@router.get("/ageing", response_class=HTMLResponse)
async def dealer_ageing(
    request: Request,
    export: str = Query(default=""),
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """Dealer ageing analysis — buckets outstanding amounts by overdue days."""
    if current_user.role not in SALES_ROLES:
        return RedirectResponse(url="/?error=Access+denied", status_code=302)

    rows = (await db.execute(
        select(DealerOrder, Dealer)
        .join(Dealer, DealerOrder.dealer_id == Dealer.id)
        .where(
            DealerOrder.due_amount > 0,
            DealerOrder.status.in_(OUTSTANDING_STATUSES),
        )
        .order_by(Dealer.business_name)
    )).all()

    now = datetime.utcnow()
    dealer_map: dict = {}
    for order, dealer in rows:
        key = str(dealer.id)
        if key not in dealer_map:
            dealer_map[key] = {
                "dealer": dealer,
                "current": 0.0,
                "d30": 0.0,
                "d60": 0.0,
                "d90": 0.0,
                "d90plus": 0.0,
                "total": 0.0,
            }
        amt = float(order.due_amount or 0)
        dealer_map[key]["total"] += amt
        if order.payment_due_date is None or order.payment_due_date >= now:
            dealer_map[key]["current"] += amt
        else:
            days = (now - order.payment_due_date).days
            if days <= 30:
                dealer_map[key]["d30"] += amt
            elif days <= 60:
                dealer_map[key]["d60"] += amt
            elif days <= 90:
                dealer_map[key]["d90"] += amt
            else:
                dealer_map[key]["d90plus"] += amt

    ageing_rows = list(dealer_map.values())
    totals = {
        k: sum(r[k] for r in ageing_rows)
        for k in ("current", "d30", "d60", "d90", "d90plus", "total")
    }

    if export == "csv":
        output = io.StringIO()
        writer = csv.writer(output)
        writer.writerow(["Dealer", "Current", "1-30 Days", "31-60 Days", "61-90 Days", "90+ Days", "Total"])
        for r in ageing_rows:
            writer.writerow([
                r["dealer"].business_name,
                f"{r['current']:.2f}",
                f"{r['d30']:.2f}",
                f"{r['d60']:.2f}",
                f"{r['d90']:.2f}",
                f"{r['d90plus']:.2f}",
                f"{r['total']:.2f}",
            ])
        writer.writerow([
            "TOTAL",
            f"{totals['current']:.2f}",
            f"{totals['d30']:.2f}",
            f"{totals['d60']:.2f}",
            f"{totals['d90']:.2f}",
            f"{totals['d90plus']:.2f}",
            f"{totals['total']:.2f}",
        ])
        filename = f"dealer-ageing-{now.strftime('%Y%m%d')}.csv"
        return StreamingResponse(
            iter([output.getvalue().encode("utf-8-sig")]),
            media_type="text/csv; charset=utf-8-sig",
            headers={"Content-Disposition": f"attachment; filename={filename}"},
        )

    return templates.TemplateResponse("dealers/ageing.html", {
        "request": request,
        "current_user": current_user,
        "ageing_rows": ageing_rows,
        "totals": totals,
        "as_of": now,
    })
```

- [ ] **Step 3.2: Create `templates/dealers/ageing.html`**

Create the file `templates/dealers/ageing.html` with the following content:

```html
{% extends "base.html" %}
{% block title %}Dealer Ageing Analysis{% endblock %}

{% block content %}
<div class="container-fluid py-4">

  <!-- Header -->
  <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-2">
    <div>
      <h4 class="mb-0 fw-semibold">
        <i class="bi bi-hourglass-split me-2 text-warning"></i>Dealer Ageing Analysis
      </h4>
      <small class="text-muted">As of {{ as_of.strftime('%d-%m-%Y %H:%M') }} UTC</small>
    </div>
    <div class="d-flex gap-2">
      <a href="?export=csv" class="btn btn-sm btn-outline-success">
        <i class="bi bi-download me-1"></i> Export CSV
      </a>
      <a href="/dealers" class="btn btn-sm btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i> Back to Dealers
      </a>
    </div>
  </div>

  {% if ageing_rows %}
  <!-- Summary Cards -->
  <div class="row g-3 mb-4">
    <div class="col-6 col-md-2">
      <div class="card border-0 shadow-sm text-center py-3">
        <div class="small text-muted mb-1">Current</div>
        <div class="fw-bold text-success fs-6">&#8377;{{ "{:,.0f}".format(totals.current) }}</div>
      </div>
    </div>
    <div class="col-6 col-md-2">
      <div class="card border-0 shadow-sm text-center py-3">
        <div class="small text-muted mb-1">1–30 Days</div>
        <div class="fw-bold text-warning fs-6">&#8377;{{ "{:,.0f}".format(totals.d30) }}</div>
      </div>
    </div>
    <div class="col-6 col-md-2">
      <div class="card border-0 shadow-sm text-center py-3">
        <div class="small text-muted mb-1">31–60 Days</div>
        <div class="fw-bold text-orange fs-6" style="color:#fd7e14">&#8377;{{ "{:,.0f}".format(totals.d60) }}</div>
      </div>
    </div>
    <div class="col-6 col-md-2">
      <div class="card border-0 shadow-sm text-center py-3">
        <div class="small text-muted mb-1">61–90 Days</div>
        <div class="fw-bold text-danger fs-6">&#8377;{{ "{:,.0f}".format(totals.d90) }}</div>
      </div>
    </div>
    <div class="col-6 col-md-2">
      <div class="card border-0 shadow-sm text-center py-3">
        <div class="small text-muted mb-1">90+ Days</div>
        <div class="fw-bold text-danger fs-6">&#8377;{{ "{:,.0f}".format(totals.d90plus) }}</div>
      </div>
    </div>
    <div class="col-6 col-md-2">
      <div class="card border-0 shadow-sm text-center py-3 border-primary">
        <div class="small text-muted mb-1">Total Outstanding</div>
        <div class="fw-bold text-primary fs-6">&#8377;{{ "{:,.0f}".format(totals.total) }}</div>
      </div>
    </div>
  </div>

  <!-- Ageing Table -->
  <div class="card border-0 shadow-sm">
    <div class="card-body p-0">
      <div class="table-responsive">
        <table class="table table-hover table-sm mb-0 align-middle">
          <thead class="table-light">
            <tr>
              <th class="ps-3">Dealer</th>
              <th class="text-end">Current<br><small class="text-muted fw-normal">(not yet due)</small></th>
              <th class="text-end">1–30 Days</th>
              <th class="text-end">31–60 Days</th>
              <th class="text-end">61–90 Days</th>
              <th class="text-end text-danger">90+ Days</th>
              <th class="text-end pe-3">Total Outstanding</th>
            </tr>
          </thead>
          <tbody>
            {% for row in ageing_rows %}
            <tr>
              <td class="ps-3 fw-semibold">
                <a href="/dealers/{{ row.dealer.id }}" class="text-decoration-none">
                  {{ row.dealer.business_name }}
                </a>
                {% if row.dealer.city %}
                <br><small class="text-muted">{{ row.dealer.city }}</small>
                {% endif %}
              </td>
              <td class="text-end">
                {% if row.current > 0 %}
                  <span class="text-success">&#8377;{{ "{:,.0f}".format(row.current) }}</span>
                {% else %}
                  <span class="text-muted">—</span>
                {% endif %}
              </td>
              <td class="text-end">
                {% if row.d30 > 0 %}
                  <span class="text-warning fw-semibold">&#8377;{{ "{:,.0f}".format(row.d30) }}</span>
                {% else %}
                  <span class="text-muted">—</span>
                {% endif %}
              </td>
              <td class="text-end">
                {% if row.d60 > 0 %}
                  <span style="color:#fd7e14" class="fw-semibold">&#8377;{{ "{:,.0f}".format(row.d60) }}</span>
                {% else %}
                  <span class="text-muted">—</span>
                {% endif %}
              </td>
              <td class="text-end">
                {% if row.d90 > 0 %}
                  <span class="text-danger fw-semibold">&#8377;{{ "{:,.0f}".format(row.d90) }}</span>
                {% else %}
                  <span class="text-muted">—</span>
                {% endif %}
              </td>
              <td class="text-end">
                {% if row.d90plus > 0 %}
                  <span class="badge bg-danger fs-6 px-2 py-1">&#8377;{{ "{:,.0f}".format(row.d90plus) }}</span>
                {% else %}
                  <span class="text-muted">—</span>
                {% endif %}
              </td>
              <td class="text-end pe-3 fw-bold">
                &#8377;{{ "{:,.0f}".format(row.total) }}
              </td>
            </tr>
            {% endfor %}
          </tbody>
          <tfoot class="table-dark">
            <tr>
              <th class="ps-3">TOTAL</th>
              <th class="text-end">&#8377;{{ "{:,.0f}".format(totals.current) }}</th>
              <th class="text-end">&#8377;{{ "{:,.0f}".format(totals.d30) }}</th>
              <th class="text-end">&#8377;{{ "{:,.0f}".format(totals.d60) }}</th>
              <th class="text-end">&#8377;{{ "{:,.0f}".format(totals.d90) }}</th>
              <th class="text-end">
                {% if totals.d90plus > 0 %}
                  <span class="text-danger fw-bold">&#8377;{{ "{:,.0f}".format(totals.d90plus) }}</span>
                {% else %}
                  &#8377;0
                {% endif %}
              </th>
              <th class="text-end pe-3">&#8377;{{ "{:,.0f}".format(totals.total) }}</th>
            </tr>
          </tfoot>
        </table>
      </div>
    </div>
  </div>

  {% else %}
  <div class="alert alert-success d-flex align-items-center" role="alert">
    <i class="bi bi-check-circle-fill me-2"></i>
    No outstanding dealer orders. All accounts are cleared.
  </div>
  {% endif %}

</div>
{% endblock %}
```

- [ ] **Step 3.3: Run Task 3 tests**

```
cd C:\Users\Pankaj.sehgal\Claude\Oxypc\oxypc-inventory
python -m pytest tests/test_sprint14_unit.py::test_dealers_ageing_route_exists tests/test_sprint14_unit.py::test_ageing_bucket_logic -v
```

**Expected output:**
```
PASSED tests/test_sprint14_unit.py::test_dealers_ageing_route_exists
PASSED tests/test_sprint14_unit.py::test_ageing_bucket_logic
2 passed in 0.xxs
```

---

## Final verification: run all 5 Sprint 14 tests

- [ ] **Step 4.1: Full test run**

```
cd C:\Users\Pankaj.sehgal\Claude\Oxypc\oxypc-inventory
python -m pytest tests/test_sprint14_unit.py -v
```

**Expected output:**
```
tests/test_sprint14_unit.py::test_base_html_overdue_nav_link     PASSED
tests/test_sprint14_unit.py::test_base_html_audit_log_nav_link   PASSED
tests/test_sprint14_unit.py::test_whatsapp_compose_route_exists  PASSED
tests/test_sprint14_unit.py::test_dealers_ageing_route_exists    PASSED
tests/test_sprint14_unit.py::test_ageing_bucket_logic            PASSED

5 passed in 0.xxs
```

- [ ] **Step 4.2: Full Sprint 13 regression (no regressions)**

```
cd C:\Users\Pankaj.sehgal\Claude\Oxypc\oxypc-inventory
python -m pytest tests/test_sprint13_unit.py -v
```

**Expected output (all 5 must remain green):**
```
tests/test_sprint13_unit.py::test_list_dealers_accepts_page_param  PASSED
tests/test_sprint13_unit.py::test_pagination_offset_calculation    PASSED
tests/test_sprint13_unit.py::test_total_pages_calculation          PASSED
tests/test_sprint13_unit.py::test_overdue_route_exists             PASSED
tests/test_sprint13_unit.py::test_admin_audit_log_route_exists     PASSED

5 passed in 0.xxs
```

- [ ] **Step 4.3: Server smoke test**

```
cd C:\Users\Pankaj.sehgal\Claude\Oxypc\oxypc-inventory
uvicorn main:app --reload --port 8000
```

Manual checks (log in as admin or sales):
1. Sidebar SALES & CRM section shows "Overdue Orders" link below "Dealers".
2. Navigate to `/dealers/overdue` — the Overdue Orders link highlights as active.
3. Navigate to `/dealers/ageing` — ageing table renders; summary cards show totals.
4. Click "Export CSV" on ageing page — browser downloads `dealer-ageing-YYYYMMDD.csv` with headers and TOTAL row.
5. From an overdue order row, click "Send Reminder" (WhatsApp icon) — browser lands on `/whatsapp/compose?order_id=<id>` with phone and message pre-filled.
6. If dealer has no phone, yellow warning banner is visible on compose page.
7. Admin sidebar shows second "Audit Log" link pointing to `/admin/audit-log`.

---

## Self-Review Checklist

| Check | Status |
|---|---|
| Route collision: `/dealers/ageing` registered before `/{dealer_id}` | Must verify in `routers/dealers.py` — `/ageing` inserted after `/overdue`, before `/new` |
| Compose route order: `/whatsapp/compose` before `""` (main page) | Must verify in `routers/whatsapp.py` — GET `/compose` inserted before `@router.get("")` |
| Sidebar role guard: "Overdue Orders" inside `{% if role in ['admin','sales','sales_manager','telecaller'] %}` | Confirmed — the insertion point is inside the enclosing block (lines 131-159) |
| Sidebar role guard: new Audit Log inside `{% if role == 'admin' %}` | Confirmed — the insertion point is before `{% endif %}` at line 289 |
| CSV export encoding: `utf-8-sig` (BOM) for Excel compatibility | Used in `StreamingResponse(iter([output.getvalue().encode("utf-8-sig")]), ...)` |
| Rupee symbol in router: `₹` escape (not raw ₹ in Python string) | Used in `prefill_message` f-string in compose route |
| Rupee symbol in templates: `&#8377;` HTML entity | Used throughout ageing.html and compose.html |
| No new models / migrations needed | Confirmed — all models (`Dealer`, `DealerOrder`) already exist |
| Empty-state handling in ageing template | `{% if ageing_rows %}` / `{% else %}` with "All accounts cleared" alert |
| Ageing bucket boundary: `days <= 30` means day 30 falls in 1-30 band (not 31-60) | Test case `(now - 30 days, "d30")` verifies this |
| `as_of.strftime('%d-%m-%Y %H:%M')` — DD-MM-YYYY format matches Indian convention | Confirmed in template header |
| `test_ageing_bucket_logic` is pure Python, no DB import, runs standalone | Confirmed — uses only `datetime` and a local `FakeOrder` class |
| Sprint 13 regression: pagination, overdue route, admin audit-log route still pass | Step 4.2 covers this |

---

## Delivery Summary

| Task | Files changed | Tests |
|---|---|---|
| 1 — Sidebar nav links | `templates/base.html` (2 insertions) | `test_base_html_overdue_nav_link`, `test_base_html_audit_log_nav_link` |
| 2 — WhatsApp compose page | `routers/whatsapp.py` (+1 route), `templates/whatsapp/compose.html` (new) | `test_whatsapp_compose_route_exists` |
| 3 — Dealer ageing report | `routers/dealers.py` (+1 route), `templates/dealers/ageing.html` (new) | `test_dealers_ageing_route_exists`, `test_ageing_bucket_logic` |
| **Total** | **5 files** | **5 tests** |
