fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
python-jose[cryptography]==3.3.0
# Password hashing calls the bcrypt API directly (auth/dependencies.py) — passlib
# was dropped from the code but left in here, and it is the ONLY thing in the tree
# incompatible with modern bcrypt: passlib 1.7.4 reads bcrypt.__about__, removed in
# bcrypt 4.1, so CryptContext.verify() raises against bcrypt 5.x. Nothing imports
# passlib, so its removal changes no behaviour and removes a live footgun.
# Pinned to the version already running in production rather than a floor, so a
# rebuild cannot silently pull a release with different truncation semantics —
# bcrypt 5.0 raises on >72-byte passwords where earlier versions truncated.
bcrypt==5.0.0
python-multipart==0.0.20
jinja2==3.1.4
aiofiles==24.1.0
openpyxl>=3.1.0
pytz>=2024.1
python-dotenv==1.0.1
slowapi==0.1.9
pyinstaller>=6.10.0
# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
pypdf
pdfplumber
fpdf2>=2.7.9
reportlab>=4.0.0
python-pptx>=1.0.0
